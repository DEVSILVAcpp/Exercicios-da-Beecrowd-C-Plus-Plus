#include <iostream>
#include <iomanip>
#include <string>
#include <cmath>
#include <stdlib.h>

//Diferença

using namespace std;

int main(){

    int A, B, C, D, DIFERENCA;
    cin >> A >> B >> C >> D;
    DIFERENCA = (A * B - C * D);
    cout << "DIFERENCA = "<< DIFERENCA << endl;

    return 0;
}
